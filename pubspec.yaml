name: emk_webview
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"  # <-- ЭТА СТРОКА ИСПРАВЛЯЕТ ОШИБКУ

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.0

flutter:
  uses-material-design: true

flutter_icons:
  ios: true
  android: true
  image_path: "assets/faviconemk.png"
