name: emk_webview
description: A new Flutter project.
publish_to: 'none'
version: 1.1.0+11   # ← ИЗМЕНЕНО: 1.1.0, сборка 6

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.8.0
  url_launcher: ^6.3.0
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_launcher_icons: ^0.14.0

flutter:
  uses-material-design: true

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/faviconemk.png"
  remove_alpha_ios: true
